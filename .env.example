# =============================================================
#  llm-caption 設定ファイル（.env.example）
#  このファイルを .env にコピーして各値を環境に合わせて編集する。
#  ハードコーディングは行わず、パスや API はすべてここで指定する。
# =============================================================

# ---- 入出力 -------------------------------------------------
# キャプションを付与する画像フォルダ（再帰的に探索される）
IMAGE_DIR=./sample

# WD-EVA02 モデル一式（*.safetensors / config.json / selected_tags.csv）の置き場所
MODEL_DIR=./model

# 除外タグリスト（*.csv）を置くフォルダ。直下の全 CSV を読み込み、
# 各行の先頭カラムのタグを Danbooru タグ出力から除外する（1 行目はヘッダ）。
# CSV が 1 つも無ければ除外は行わない。入手先は README.md を参照。
IGNORE_TAGS_DIR=./ignore_tags

# ---- WD-EVA02 タガー ---------------------------------------
# 一般タグ採用のしきい値（0.0-1.0、低いほど多くのタグを採用）
GENERAL_THRESHOLD=0.35
# キャラクタータグ採用のしきい値
CHARACTER_THRESHOLD=0.85
# レーティング（general/sensitive/questionable/explicit）を先頭に含めるか
INCLUDE_RATINGS=false
# タグ内のアンダースコアを半角スペースに置換するか（例: long_hair -> long hair）
REPLACE_UNDERSCORE=true
# 括弧を \( \) にエスケープするか（SD 系学習向け）
ESCAPE_PARENTHESES=true

# ---- LLM（OpenAI 互換 API）---------------------------------
# ローカル llama-server の例: http://localhost:8080/v1
# 外部サーバーを使う場合はその base URL に変更する
LLM_BASE_URL=http://localhost:8080/v1
# API キー。ローカル llama-server では任意の文字列でよい
LLM_API_KEY=local
# モデル名。llama-server では任意でよいが、外部 API では正式名を指定する
LLM_MODEL=gemma
# 生成パラメータ
LLM_TEMPERATURE=0.4
LLM_MAX_TOKENS=1024
# 画像を LLM に渡すか（マルチモーダル）。
#   true  = 画像 + タグを渡す（llama-server 側に --mmproj が必要）
#   false = タグのみ渡す（mmproj 不要）
LLM_SEND_IMAGE=true
# 思考（reasoning）付きモデルの思考を無効化する。
#   true  = 思考オフ。回答のみを高速に得る（reasoning モデルでは必須級）
#   false = 思考あり。その場合 LLM_MAX_TOKENS を大きめ（1024+）にすること
LLM_DISABLE_THINKING=true
# 生成するキャプションの言語
CAPTION_LANGUAGE=English
# システムプロンプトを上書きしたい場合のみ設定（空なら既定プロンプトを使用）
LLM_SYSTEM_PROMPT=
